name: pythagorean-triplet
version: 1.0.0.4

dependencies:
  - base

library:
  exposed-modules: Triplet
  source-dirs: src
  ghc-options: -Wall
  dependencies:
    - arithmoi

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - pythagorean-triplet
      - hspec
